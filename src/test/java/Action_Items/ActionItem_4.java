// package Action_Items;

import io.github.bonigarcia.wdm.WebDriverManager;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.support.ui.Select;

import java.util.ArrayList;

//public class ActionItem_4   {
    //public static void main(String[] args) {

       // WebDriverManager.chromedriver().setup();
       //  ChromeOptions options = new ChromeOptions();
        // options.addArguments("start-maximized");
        // WebDriver driver = new ChromeDriver(options);

       // ArrayList<String> destination = new ArrayList<>();
        // destination.add("New York");
       //  destination.add("Texas");
        // destination.add("New Jersey");

      //  ArrayList<Integer> Adult = new ArrayList<>();
        // Adult.add(2);
        // Adult.add(2);
        // Adult.add(3);

        // ArrayList<String> childAge = new ArrayList();
        //  childAge.add("2");
        //  childAge.add("4");
        // childAge.add("6");


        //   for (int i = 0; i < destination.size(); i++) {

        //   driver.navigate().to("https://www.hotels.com/");
        //wait two seconds
        //   Thread.sleep(2000);
        //   driver.findElement(By.xpath("//*[@aria-label='Going to']")).click();
        //wait two seconds
        //   Thread.sleep(2000);
        //   WebElement search = driver.findElement(By.xpath("//*[@placeholder='Going to']"));
        //  search.sendKeys(destination.get(i));
        //wait two seconds
        // Thread.sleep(2000);

        //select destination from dropdown option
        //  driver.findElements(By.xpath("//*[@class='trucate']")).get(0).click();

        //click travelers and set the adult option back to one
        //       driver.findElement(By.xpath("//*[contains(@class, 'uitk-menu-triggerutik')]")).click();
        //wait two seconds
        //   Thread.sleep(2000);

        //select number of adult option (1)
        //   driver.findElement(By.xpath("//*[@class= 'uitk-step-input-button']")).click();
        //wait two seconds
        //   Thread.sleep(2000);

        //   for (int a = 0; a< Adult.size(); a++) {
        // driver.findElement(By.xpath("//*[@aria-label='Increase number of adults to 2']")).click();
        //}//end of adult loop

        //click to add one child
    //driver.findElement(By.xpath("//*[@aria-label= 'Increase the number of children to 1']")).click();

        //click (select) option for children
        //   WebElement Child = driver.findElement(By.xpath("//*[@id= 'age-traveler_selector_children_age_slector-0-0']"));
//  Select childDropdown = new Select(Child);

        //select by value


    //}//end of main
//}//end of java class
